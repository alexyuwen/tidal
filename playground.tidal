


-- Ply 2.
let
slicesTotal = 8 * 24
in do
  setcps (134/60/4)
  d1 $ "cp" # gain 0.8
  d2 $ slice slicesTotal (8 * (slow 24 $ run 24) + (run 8)) $ loopAt 24 $ s "bach"
  d2 $ slice slicesTotal (8 * (slow 24 $ run 24) + (run 8)) $ loopAt 24 $ s "bach"




